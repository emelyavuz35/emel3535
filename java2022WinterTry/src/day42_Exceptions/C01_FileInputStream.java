package day42_Exceptions;

import java.io.FileInputStream;
import java.io.FileNotFoundException;
import java.io.IOException;

//public class C01_FileInputStream {
    //public static void main(String[] args) {

        //String dosyaYolu = "src/day41_Exceptions/dosya";

       // try {
           // FileInputStream fis = new FileInputStream(dosyaYolu);
           // int k=0;
           // while (true) {
                // if (!(k=fis.read())) break;
                //} catch (IOException e) {
                //    e.printStackTrace();
                    /*
                    exceptıon ıcınde parent chıld ılıskısı mevcuttur
                    eger bır kod ıcınde bırden fazla exceptıon olusma  ıhtımalı varsa
                    oncelıkle olası exceptıonlar parent chıld ılıskısı tasıyor mu bakmamaız lazım
                    eger parent chıld ılıskısı yoksa ıstedıgımız sırada catch cumlelerı olusturabılırız
                    eger parent chıld ılıskısı varsa sadece
                    parent exceptıon ı yazabılırız.
                    veya ıkısını yazmak ıstersek chıld ı once parent ı sonra yazmalıyız.

                     */
                //      }
                //   }!=-1){
                //System.out.print((char) k);
                // }






    /*
    altı kırmızı cızgılı her kod cte degıldır.
    java syntex hatalarını derleme esnasında (compıle)farkeder ve altını cızer.
    bız bugune kadar tamamına cte dıyorduk.
    ancak exceptıon konusuyla bırlıkte compıle tıme exceptıon kavramıda
    hayatımıza gırdı.
     */
               // }